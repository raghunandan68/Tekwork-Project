import React from 'react';
import ReactDOM from 'react-dom/client';
import SmartShelfAI from './SmartShelfAI';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SmartShelfAI />
  </React.StrictMode>
);